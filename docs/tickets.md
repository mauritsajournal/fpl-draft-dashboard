# Ticket Backlog — FPL Draft Dashboard

> Generated by Software Architect Agent
> Date: 2026-03-12
> Total tickets: 24
> Phases: 3

---

## Phase 1 — MVP (Foundation + Core Views)

---

## [x] T-001 — Project scaffolding with Astro, Tailwind, and TypeScript
> DONE 2026-03-12 — Scaffolded Astro 5 + Svelte 5 + Tailwind 4 + Vitest. All pages stubbed, build passes.

**Phase:** 1 (MVP)
**Component:** Project setup
**Depends on:** None
**Estimated effort:** M
**Priority:** P0 (critical path)

**Description:**
Initialize the Astro project with TypeScript, Tailwind CSS, and Svelte integration. Set up the folder structure as defined in the technical blueprint. Configure ESLint, Prettier, and Vitest. Create the base layout component with responsive navigation for all dashboard pages.

**Acceptance criteria:**
- [ ] Astro 5.x project initialized with TypeScript strict mode
- [ ] `@astrojs/svelte` adapter installed and configured for Svelte 5
- [ ] Tailwind CSS 4.x configured with Astro integration
- [ ] ESLint + Prettier configured with Astro/Svelte plugins
- [ ] Vitest configured and a sample test passes
- [ ] Folder structure matches blueprint: `src/pages/`, `src/components/`, `src/layouts/`, `scripts/`, `data/`
- [ ] Base layout (`src/layouts/Layout.astro`) with responsive nav, footer with "Last updated" placeholder, and proper HTML head (favicon, OG tags, meta description)
- [ ] All pages from blueprint have placeholder files that render with the layout
- [ ] `npm run dev` starts the dev server successfully
- [ ] `npm run build` produces a static site in `dist/`

**Technical notes:**
Use `npm create astro@latest` with the empty template. Add integrations:
```bash
npx astro add svelte tailwind
npm install -D eslint prettier vitest @eslint/js eslint-plugin-astro prettier-plugin-astro
```
Reference: technical-blueprint.md §2.1, §2.2, §3.3, §3.5

**Files likely affected:**
- `package.json`
- `astro.config.mjs`
- `tsconfig.json`
- `tailwind.config.mjs`
- `.prettierrc`
- `eslint.config.js`
- `vitest.config.ts`
- `src/layouts/Layout.astro`
- `src/pages/index.astro` (and all other page stubs)

---

## [x] T-002 — GitHub Actions workflow: fetch-and-deploy
> DONE 2026-03-12 — Created fetch-and-deploy, deploy-on-code-change, and keepalive workflows.

**Phase:** 1 (MVP)
**Component:** CI/CD
**Depends on:** T-001
**Estimated effort:** M
**Priority:** P0 (critical path)

**Description:**
Create the combined fetch-and-deploy GitHub Actions workflow that runs on a cron schedule (01:00, 07:00, 13:00, 19:00 UTC) and on manual dispatch. This workflow fetches data, commits changes, builds the Astro site, and deploys to GitHub Pages — all in one workflow to avoid the GITHUB_TOKEN limitation. Also create the deploy-on-code-change workflow and the keepalive workflow.

**Acceptance criteria:**
- [ ] `.github/workflows/fetch-and-deploy.yml` created with cron schedule `0 1,7,13,19 * * *`
- [ ] Workflow has `workflow_dispatch` trigger for manual runs
- [ ] Workflow has correct permissions: `contents: write`, `pages: write`, `id-token: write`
- [ ] Workflow steps: checkout, setup Node.js 22, install deps, run fetch, run transform, check git diff, conditional commit, build Astro, deploy to Pages
- [ ] Data commit uses the `actions/github-script` or git commands with proper author config
- [ ] `.github/workflows/deploy.yml` created for code-only pushes (paths: src/**, astro.config.*, package.json, tailwind.config.*)
- [ ] `.github/workflows/keepalive.yml` created with weekly Monday cron
- [ ] GitHub Pages enabled in repo settings (source: GitHub Actions)

**Technical notes:**
Critical: commits made with GITHUB_TOKEN do NOT trigger other workflows. That's why fetch + build + deploy must be in ONE workflow. The deploy-on-code-change workflow handles developer pushes to src/.

For GitHub Pages deployment, use the official approach:
```yaml
- uses: actions/configure-pages@v5
- uses: actions/upload-pages-artifact@v3
  with:
    path: dist/
- uses: actions/deploy-pages@v4
```

Reference: technical-blueprint.md §7.2

**Files likely affected:**
- `.github/workflows/fetch-and-deploy.yml`
- `.github/workflows/deploy.yml`
- `.github/workflows/keepalive.yml`

---

## [x] T-003 — TypeScript types for FPL Draft API responses
> DONE 2026-03-12 — All API and dashboard types defined with runtime validation helper.

**Phase:** 1 (MVP)
**Component:** Data Pipeline
**Depends on:** T-001
**Estimated effort:** M
**Priority:** P0 (critical path)

**Description:**
Define TypeScript interfaces for all FPL Draft API responses and for the internal dashboard data structures. These types are the foundation for type-safe data fetching and transformation.

**Acceptance criteria:**
- [ ] `scripts/types/api.ts` — Types for all 8 API endpoints' response shapes (verified against real API)
- [ ] `scripts/types/dashboard.ts` — Types for `DashboardData` interface and all sub-types (Standing, Manager, H2HRecord, PlayerStat, etc.)
- [ ] `scripts/types/meta.ts` — Type for `FetchMeta` interface
- [ ] All types are exported and importable from other scripts
- [ ] A validation function `validateApiResponse<T>(data: unknown, requiredKeys: string[]): T` exists for runtime type checking

**Technical notes:**
API response structures are documented and verified in technical-blueprint.md §5.1-5.8. Key structures:
- Bootstrap: `{ elements: Player[] }` with xG/xA fields as strings (e.g., "6.84")
- League details: `{ league, league_entries, matches, standings }`
- Element status: `{ element_status: { element, owner, status, in_accepted_trade }[] }`
- Entry event: `{ picks, entry_history, subs }`
- Live event: `{ elements: { [id]: { stats: {...} } } }`
- Transactions: array of `{ id, entry, event, element_in, element_out, kind, result }`
- Draft choices: `{ choices: [...] }`

Reference: technical-blueprint.md §3.2, §4.1, §5.1-5.8

**Files likely affected:**
- `scripts/types/api.ts`
- `scripts/types/dashboard.ts`
- `scripts/types/meta.ts`
- `scripts/types/index.ts`

---

## [x] T-004 — Data fetch scripts for all API endpoints
> DONE 2026-03-12 — All 8 fetch scripts + orchestrator with incremental caching, exponential backoff. Verified against live API.

**Phase:** 1 (MVP)
**Component:** Data Pipeline
**Depends on:** T-003
**Estimated effort:** L
**Priority:** P0 (critical path)

**Description:**
Implement all 8 fetch scripts that call FPL Draft API endpoints and write JSON to the `data/` directory. Implement incremental fetching for picks and live data using `data/meta.json` state tracking. Handle errors with exponential backoff.

**Acceptance criteria:**
- [ ] `scripts/fetch-bootstrap.ts` fetches and writes `data/players.json`
- [ ] `scripts/fetch-league.ts` fetches and writes `data/league.json`
- [ ] `scripts/fetch-element-status.ts` fetches and writes `data/ownership.json`
- [ ] `scripts/fetch-picks.ts` fetches and writes `data/picks/gw{XX}.json` — only for new GWs since meta.lastFetchedGameweek
- [ ] `scripts/fetch-live.ts` fetches and writes `data/live/gw{XX}.json` — only for new GWs, re-fetches current in-progress GW
- [ ] `scripts/fetch-transactions.ts` fetches and writes `data/transactions.json`
- [ ] `scripts/fetch-draft.ts` fetches and writes `data/draft.json` — only if meta.draftFetched is false
- [ ] `scripts/fetch-fixtures.ts` fetches and writes `data/fixtures.json`
- [ ] `scripts/fetch-all.ts` orchestrates all fetches in sequence with 500ms delays between endpoints
- [ ] `data/meta.json` is created/updated with lastFetchedGameweek, lastUpdated, draftFetched
- [ ] Exponential backoff: 3 retries with 2s, 8s, 32s delays
- [ ] API responses are validated (HTTP 200 + expected JSON keys) before writing to disk
- [ ] Failed fetches do NOT overwrite existing data files
- [ ] All scripts are runnable via `npx tsx scripts/fetch-all.ts`

**Technical notes:**
Use Node.js built-in `fetch()` (Node 22). No external HTTP libraries needed.

League ID: 820. Entry IDs for the 8 managers must be read from `data/league.json` (league_entries array), not hardcoded. The initial run will need to fetch league.json first to get entry IDs, then fetch picks/live for all completed GWs.

For the initial seed (GW1-29): 232 pick requests + 29 live requests = 261 requests total. With rate limiting: ~5 minutes. GitHub Actions has a 6-hour timeout, so this is fine.

Batch picks fetch pattern:
```typescript
for (const gw of newGameweeks) {
  const gwPicks = {};
  for (const entry of leagueEntries) {
    await delay(200);
    gwPicks[entry.entry_id] = await fetchWithRetry(`/api/entry/${entry.entry_id}/event/${gw}`);
  }
  writeJson(`data/picks/gw${String(gw).padStart(2, '0')}.json`, gwPicks);
  await delay(1000); // delay between GWs
}
```

Reference: technical-blueprint.md §3.1, §5.0-5.8

**Files likely affected:**
- `scripts/fetch-bootstrap.ts`
- `scripts/fetch-league.ts`
- `scripts/fetch-element-status.ts`
- `scripts/fetch-picks.ts`
- `scripts/fetch-live.ts`
- `scripts/fetch-transactions.ts`
- `scripts/fetch-draft.ts`
- `scripts/fetch-fixtures.ts`
- `scripts/fetch-all.ts`
- `scripts/lib/fetch-utils.ts` (shared retry/delay/validate logic)
- `data/meta.json`

---

## [x] T-005 — Data transformation layer (transform.ts)
> DONE 2026-03-12 — Full transform pipeline producing dashboard.json with all sections. Tested against live data.

**Phase:** 1 (MVP)
**Component:** Data Pipeline
**Depends on:** T-004
**Estimated effort:** XL
**Priority:** P0 (critical path)

**Description:**
Implement the transform script that reads all raw JSON data files and produces `data/dashboard.json` — a pre-computed data structure optimized for dashboard rendering. This is the core business logic of the project.

**Acceptance criteria:**
- [ ] `scripts/transform.ts` reads all data files and produces `data/dashboard.json`
- [ ] Validates file existence and freshness before processing
- [ ] Sets `dataAvailable` flags per section — missing non-critical data produces degraded output, not failure
- [ ] If critical files (league.json, players.json) are missing, exits with error code 1
- [ ] Computes `standings` from league.json matches and standings data
- [ ] Computes `managers` with aggregated stats: total points, average per GW, best GW, worst GW, current form
- [ ] Computes `gameweekHistory` — snapshot of standings after each completed GW for trend charts
- [ ] Computes `h2hMatrix` — 8x8 matrix of all head-to-head records between managers
- [ ] Computes `playerStats` — top players owned in the league with per-GW breakdown
- [ ] Computes `freeAgents` — best unowned players by total_points, form, xG+xA
- [ ] Computes `powerRankings` — team strength score based on player form, xG, total points
- [ ] Processes `transactions` into a structured timeline with player names resolved
- [ ] Processes `draftPicks` with player names resolved
- [ ] Computes `predictions` — next GW expected points per team based on player xG/xA and fixture difficulty
- [ ] Computes `benchAnalysis` — total bench player points per manager per GW (simple version)
- [ ] Records `fetchErrors` from last fetch cycle
- [ ] All computations have unit tests (see T-007)

**Technical notes:**
Player name resolution: join element IDs from picks/transactions/draft with players.json to get `web_name`.

Power ranking algorithm suggestion: weighted sum of (player form * 0.3 + player xGI per 90 * 0.3 + total points * 0.2 + fixture difficulty * 0.2). Normalize to 0-100 scale.

Prediction algorithm: for each player in a team's starting XI, calculate expected points based on: position-specific baseline + xG-based goal probability + xA-based assist probability + clean sheet probability (from fixture difficulty). This is a rough heuristic, not a model.

Reference: technical-blueprint.md §3.2

**Files likely affected:**
- `scripts/transform.ts`
- `scripts/lib/standings.ts`
- `scripts/lib/h2h.ts`
- `scripts/lib/power-rankings.ts`
- `scripts/lib/predictions.ts`
- `scripts/lib/bench-analysis.ts`
- `scripts/lib/player-resolver.ts`
- `data/dashboard.json`

---

## [x] T-006 — League standings page (home)
> DONE 2026-03-12 — Full standings table with rank changes, stat cards, GW performance cards. Maurits highlighted.

**Phase:** 1 (MVP)
**Component:** Frontend
**Depends on:** T-001, T-005
**Estimated effort:** M
**Priority:** P0 (critical path)

**Description:**
Build the home page showing the current league standings table, current gameweek results, and key league stats. This is the landing page of the dashboard.

**Acceptance criteria:**
- [ ] `src/pages/index.astro` renders the league standings table
- [ ] Table shows: rank, team name, player name, P (played), W, D, L, PF (points for), PA (points against), Pts (league points)
- [ ] Highlight Maurits Koot's row (entry_id 27628 / league_entry 27137) with a subtle visual indicator
- [ ] Current gameweek H2H matchup results shown (scores, who won)
- [ ] Key stats cards: top scorer this GW, longest winning streak, biggest victory, closest match
- [ ] Data freshness indicator in footer: "Last updated: [relative time]" with warning if >24h stale
- [ ] Responsive: table scrolls horizontally on mobile, cards stack vertically
- [ ] Page has proper title and OG meta tags

**Technical notes:**
Import `dashboard.json` at build time in Astro:
```typescript
import dashboardData from '../../data/dashboard.json';
```
Astro processes this at build time — no client-side fetch needed.

For the standings table, a static Astro component is sufficient (no Svelte island needed). Only add interactivity (sortable columns) if time allows.

Reference: technical-blueprint.md §3.3

**Files likely affected:**
- `src/pages/index.astro`
- `src/components/StandingsTable.astro`
- `src/components/GameweekResults.astro`
- `src/components/StatCard.astro`

---

## [ ] T-007 — Unit tests for transformation logic

**Phase:** 1 (MVP)
**Component:** Testing
**Depends on:** T-005
**Estimated effort:** M
**Priority:** P1 (important)

**Description:**
Write unit tests for all transformation functions using Vitest. Use fixture data based on real API responses from league 820.

**Acceptance criteria:**
- [ ] Test fixtures created from real API response samples in `tests/fixtures/`
- [ ] Tests for standings calculation (correct W/D/L counting, point sorting)
- [ ] Tests for H2H matrix computation (correct win/loss/draw records)
- [ ] Tests for power ranking algorithm (sensible scores, correct ordering)
- [ ] Tests for prediction calculations (reasonable expected points)
- [ ] Tests for bench analysis (correct bench point summation)
- [ ] Tests for player name resolution (correct element ID to name mapping)
- [ ] Tests for edge cases: drawn matches, GW with 0 points, manager with no completed GWs
- [ ] Tests for degraded output when non-critical data is missing
- [ ] All tests pass with `npm test`
- [ ] Coverage >80% for `scripts/lib/` functions

**Technical notes:**
Create a `tests/fixtures/` directory with trimmed copies of real API responses (just enough entries to test logic). This avoids brittle mocks while keeping test data manageable.

Reference: technical-blueprint.md §8.1

**Files likely affected:**
- `tests/transform.test.ts`
- `tests/standings.test.ts`
- `tests/h2h.test.ts`
- `tests/predictions.test.ts`
- `tests/fixtures/*.json`

---

## [x] T-008 — Head-to-head comparison view
> DONE 2026-03-12 — H2H matrix with color coding, manager comparison cards with stats.

**Phase:** 1 (MVP)
**Component:** Frontend
**Depends on:** T-006
**Estimated effort:** L
**Priority:** P0 (critical path)

**Description:**
Build the H2H page with an interactive matrix showing all-vs-all records, and a detailed comparison view where users can select two managers for side-by-side stats.

**Acceptance criteria:**
- [ ] `src/pages/h2h.astro` renders the H2H section
- [ ] H2H matrix grid (8x8) showing W-D-L record between every pair of managers
- [ ] Color coding: green for positive record, red for negative, neutral for even
- [ ] Clicking a cell or selecting two managers opens a detailed comparison
- [ ] Detailed comparison shows: overall H2H record, per-GW scores, average points, total points, best GW, worst GW
- [ ] Radar chart comparing two managers across dimensions (total points, avg points, wins, xG, best GW score)
- [ ] Manager selection dropdowns default to Maurits Koot vs. league leader
- [ ] Responsive design: matrix becomes scrollable on mobile

**Technical notes:**
The H2H matrix is a Svelte island (`H2HMatrix.svelte`) for interactivity. The radar chart uses Chart.js via a `RadarChart.svelte` island. The comparison view uses `ManagerCompare.svelte`.

Pass data as JSON props to Svelte components:
```astro
<H2HMatrix client:load data={JSON.stringify(dashboardData.h2hMatrix)} />
```

Reference: technical-blueprint.md §3.3, §3.4

**Files likely affected:**
- `src/pages/h2h.astro`
- `src/components/H2HMatrix.svelte`
- `src/components/ManagerCompare.svelte`
- `src/components/RadarChart.svelte`

---

## Phase 2 — Full Feature Set

---

## [ ] T-009 — Team rosters and squad viewer

**Phase:** 2 (Full Feature Set)
**Component:** Frontend
**Depends on:** T-006
**Estimated effort:** M
**Priority:** P1 (important)

**Description:**
Build the teams overview page showing all 8 teams' rosters, and individual team detail pages with full squad information and player stats.

**Acceptance criteria:**
- [ ] `src/pages/teams/index.astro` shows an overview of all 8 teams with key stats
- [ ] `src/pages/teams/[slug].astro` generates a page per manager using Astro's dynamic routes
- [ ] Each team page shows: starting XI and bench (positions: GK, DEF, MID, FWD)
- [ ] Player cards show: name, position, team, total points, form, goals, assists, xG, xA
- [ ] Team aggregate stats: total points, average per GW, points trajectory
- [ ] Visual squad formation layout (optional: pitch graphic or positional grid)
- [ ] Responsive card grid on mobile

**Technical notes:**
Generate slugs from team names (slugify). Use Astro's `getStaticPaths()` for dynamic routes:
```typescript
export function getStaticPaths() {
  return dashboardData.managers.map(m => ({
    params: { slug: slugify(m.teamName) },
    props: { manager: m }
  }));
}
```

Reference: technical-blueprint.md §3.3

**Files likely affected:**
- `src/pages/teams/index.astro`
- `src/pages/teams/[slug].astro`
- `src/components/SquadView.astro`
- `src/components/PlayerCard.astro`

---

## [ ] T-010 — Gameweek trend charts

**Phase:** 2 (Full Feature Set)
**Component:** Frontend
**Depends on:** T-006
**Estimated effort:** M
**Priority:** P1 (important)

**Description:**
Build the trends page with interactive line/area charts showing how managers' positions and points have evolved over the season.

**Acceptance criteria:**
- [ ] `src/pages/trends.astro` renders trend charts
- [ ] Line chart: league position over time (GW on x-axis, position 1-8 on y-axis, inverted)
- [ ] Line chart: cumulative points over time
- [ ] Bar chart: points per gameweek per manager (stacked or grouped)
- [ ] Win/loss streak visualization
- [ ] Charts are interactive: hover for tooltips with exact values
- [ ] Toggle individual managers on/off in the chart legend
- [ ] Responsive: charts resize properly on mobile

**Technical notes:**
Use Chart.js line charts via `LineChart.svelte`. Import Chart.js with tree-shaking:
```typescript
import { Chart, LineController, LineElement, PointElement, LinearScale, CategoryScale, Tooltip, Legend } from 'chart.js';
Chart.register(LineController, LineElement, PointElement, LinearScale, CategoryScale, Tooltip, Legend);
```

Reference: technical-blueprint.md §3.4

**Files likely affected:**
- `src/pages/trends.astro`
- `src/components/LineChart.svelte`
- `src/components/BarChart.svelte`

---

## [ ] T-011 — Player statistics view

**Phase:** 2 (Full Feature Set)
**Component:** Frontend
**Depends on:** T-006
**Estimated effort:** M
**Priority:** P1 (important)

**Description:**
Build the player statistics page with sortable, filterable tables showing player performance within the league.

**Acceptance criteria:**
- [ ] `src/pages/players.astro` renders the player stats view
- [ ] Table columns: name, position, PL team, owner, total points, goals, assists, xG, xA, form, minutes
- [ ] Sortable by any column (click header to sort)
- [ ] Filterable by position (GK/DEF/MID/FWD) and by ownership status (owned/available)
- [ ] Search by player name
- [ ] Show top 100 players by default, with "Show all" option
- [ ] Highlight players owned by Maurits Koot
- [ ] Responsive: table scrolls horizontally on mobile

**Technical notes:**
Use `PlayerTable.svelte` island for interactivity (sort, filter, search). Pass full player data as JSON prop. All filtering/sorting happens client-side.

Reference: technical-blueprint.md §3.4

**Files likely affected:**
- `src/pages/players.astro`
- `src/components/PlayerTable.svelte`

---

## [ ] T-012 — Point predictions view

**Phase:** 2 (Full Feature Set)
**Component:** Frontend
**Depends on:** T-005
**Estimated effort:** M
**Priority:** P1 (important)

**Description:**
Build the predictions page showing expected points for each team in the next gameweek, based on player xG/xA from the API and upcoming fixture difficulty.

**Acceptance criteria:**
- [ ] `src/pages/predictions.astro` renders predictions for the next GW
- [ ] Shows each team's predicted points with a breakdown by player
- [ ] Predicted H2H matchup outcomes (who is favored to win each match)
- [ ] Fixture difficulty indicator per team (easy/medium/hard based on FDR)
- [ ] Per-player prediction: expected goals contribution + clean sheet probability
- [ ] Ranked list of teams by predicted points
- [ ] Clear disclaimer that predictions are rough estimates
- [ ] Graceful degradation if fixture data is unavailable

**Technical notes:**
Prediction data comes from `dashboard.json.predictions`, pre-computed by the transform step.
Display using `PredictionCard.svelte` components.

Reference: technical-blueprint.md §3.3, §3.4

**Files likely affected:**
- `src/pages/predictions.astro`
- `src/components/PredictionCard.svelte`
- `src/components/FixtureDifficulty.astro`

---

## [ ] T-013 — Power rankings view

**Phase:** 2 (Full Feature Set)
**Component:** Frontend
**Depends on:** T-005
**Estimated effort:** S
**Priority:** P1 (important)

**Description:**
Build a power rankings section showing a calculated "team strength" score per manager, with a visual breakdown of contributing factors.

**Acceptance criteria:**
- [ ] Power rankings displayed as a ranked list with scores (0-100)
- [ ] Visual breakdown: horizontal stacked bar showing contribution of form, xGI, total points, fixture difficulty
- [ ] Can be on a dedicated page or integrated into the home page as a card/section
- [ ] Rankings update automatically with each data refresh

**Technical notes:**
Power ranking data is pre-computed in `dashboard.json.powerRankings`. This is primarily a display task.

Reference: technical-blueprint.md §3.2 (power ranking algorithm)

**Files likely affected:**
- `src/pages/index.astro` (or `src/pages/power-rankings.astro`)
- `src/components/PowerRankings.astro`

---

## [ ] T-014 — Waiver/trade history and draft recap

**Phase:** 2 (Full Feature Set)
**Component:** Frontend
**Depends on:** T-005
**Estimated effort:** M
**Priority:** P1 (important)

**Description:**
Build the transfers page showing waiver/trade timeline and the original draft recap.

**Acceptance criteria:**
- [ ] `src/pages/transfers.astro` renders two sections: Transfers and Draft
- [ ] Transfer timeline: chronological list of all waivers/trades grouped by GW
- [ ] Each transfer shows: manager, player in, player out, type (waiver/free agent), result (accepted/rejected)
- [ ] Draft recap: round-by-round table showing who picked whom
- [ ] Draft stats: auto-pick count per manager, fastest/slowest picks, best draft value (points per draft position)
- [ ] Color coding for accepted vs rejected waivers

**Technical notes:**
Data comes from `dashboard.json.transactions` and `dashboard.json.draftPicks`, both with player names pre-resolved by the transform step.

Reference: technical-blueprint.md §3.3

**Files likely affected:**
- `src/pages/transfers.astro`
- `src/components/TransferTimeline.astro`
- `src/components/DraftRecap.astro`

---

## [ ] T-015 — Free agents comparison view

**Phase:** 2 (Full Feature Set)
**Component:** Frontend
**Depends on:** T-011
**Estimated effort:** S
**Priority:** P1 (important)

**Description:**
Build the free agents page showing the best unowned players available for waiver pickup.

**Acceptance criteria:**
- [ ] `src/pages/free-agents.astro` renders a sortable table of unowned players
- [ ] Table columns: name, position, PL team, total points, form, goals, assists, xG, xA, minutes
- [ ] Sortable by any column
- [ ] Filterable by position
- [ ] Top 50 shown by default
- [ ] "Waiver gems" callout — unowned players with high recent form

**Technical notes:**
Reuse the `PlayerTable.svelte` component from T-011 with different data (freeAgents from dashboard.json). Pass an `isOwnedView={false}` prop to hide the owner column.

Reference: technical-blueprint.md §3.3

**Files likely affected:**
- `src/pages/free-agents.astro`

---

## [ ] T-016 — Data health / status page

**Phase:** 2 (Full Feature Set)
**Component:** Frontend
**Depends on:** T-005
**Estimated effort:** S
**Priority:** P2 (nice to have)

**Description:**
Build a status page showing data health: last successful fetch per data source, data freshness, and any fetch errors from the last cycle.

**Acceptance criteria:**
- [ ] `src/pages/status.astro` renders data health information
- [ ] Shows last updated timestamp (from dashboard.json.meta.lastUpdated)
- [ ] Shows data availability per section (from dashboard.json.meta.dataAvailable)
- [ ] Shows any fetch errors from last cycle (from dashboard.json.meta.fetchErrors)
- [ ] Green/yellow/red indicators for data freshness (<6h / <24h / >24h)
- [ ] Link to GitHub Actions runs page for manual troubleshooting

**Technical notes:**
Simple static page, no Svelte islands needed. Read everything from dashboard.json.meta.

**Files likely affected:**
- `src/pages/status.astro`

---

## Phase 3 — Polish & Advanced Features

---

## [ ] T-017 — Bench points lost analysis

**Phase:** 3 (Polish)
**Component:** Frontend + Transform
**Depends on:** T-005
**Estimated effort:** M
**Priority:** P2 (nice to have)

**Description:**
Build a view showing how many points each manager left on the bench per gameweek. Phase 1: simple sum of bench player points. Phase 2 (optional): accurate simulation of FPL auto-substitution rules.

**Acceptance criteria:**
- [ ] Bench points data shown per manager per GW (bar chart or table)
- [ ] Cumulative bench points lost over the season
- [ ] Ranking: who has lost the most bench points
- [ ] Simple version: total points scored by players in bench positions (11-15)
- [ ] Clear label indicating this is "total bench points" not "actual substitution impact"

**Technical notes:**
Bench players are positions 12-15 in the picks data. Points come from cross-referencing with live GW data.

Accurate substitution simulation (optional enhancement): a bench player only "comes on" if a starting player (pos 1-11) played 0 minutes, following position and bench order rules. This requires understanding FPL's auto-sub priority.

Reference: technical-blueprint.md §3.2 (benchAnalysis note)

**Files likely affected:**
- `src/pages/trends.astro` (or dedicated section)
- `src/components/BenchAnalysis.svelte`
- `scripts/lib/bench-analysis.ts` (if enhancing substitution logic)

---

## [ ] T-018 — What-if fixture schedule analysis

**Phase:** 3 (Polish)
**Component:** Transform + Frontend
**Depends on:** T-005
**Estimated effort:** L
**Priority:** P2 (nice to have)

**Description:**
Implement a "what-if" analysis that calculates what league position each manager would be in if they had played against every other manager's schedule. This reveals how much luck/fixture scheduling influenced standings.

**Acceptance criteria:**
- [ ] Transform computes what-if standings for each manager against every other manager's schedule
- [ ] Frontend shows: actual position vs. average what-if position for each manager
- [ ] "Luckiest" and "unluckiest" managers highlighted
- [ ] Table showing: manager, actual rank, best what-if rank, worst what-if rank, average what-if rank
- [ ] Explanation of methodology

**Technical notes:**
Algorithm: for each manager M, simulate the season as if M had played manager X's opponents instead of their own. Use M's actual GW scores but match them against the opponents' scores from X's schedule. Repeat for all 8 schedules, compute W/D/L and league points for each simulation.

This is computationally simple (8x8x29 comparisons) but the logic needs careful implementation.

Reference: Inspired by FantasyFooty's "what-if" feature

**Files likely affected:**
- `scripts/lib/what-if.ts`
- `scripts/transform.ts` (add what-if to dashboard.json)
- `src/components/WhatIfAnalysis.svelte`

---

## [ ] T-019 — Performance optimization and Lighthouse audit

**Phase:** 3 (Polish)
**Component:** Frontend
**Depends on:** T-010, T-011
**Estimated effort:** M
**Priority:** P2 (nice to have)

**Description:**
Optimize the dashboard for performance. Target Lighthouse score >90. Optimize Chart.js bundle size, lazy-load heavy components, and ensure fast initial load.

**Acceptance criteria:**
- [ ] Lighthouse performance score >90 on all pages
- [ ] Chart.js tree-shaken — only used chart types and plugins imported
- [ ] Svelte islands use `client:visible` instead of `client:load` where appropriate (below-fold charts)
- [ ] Images optimized (if any — logos, favicons)
- [ ] Total page weight <500 KB per route (including chart JS)
- [ ] Astro build time <60 seconds

**Technical notes:**
Chart.js tree-shaking pattern:
```typescript
import { Chart as ChartJS } from 'chart.js/auto'; // DON'T do this
// Instead:
import { Chart, LineController, ... } from 'chart.js';
Chart.register(LineController, ...);
```

Svelte island loading strategies:
- `client:load` — loads immediately (use for above-fold interactive content)
- `client:visible` — loads when scrolled into view (use for below-fold charts)
- `client:idle` — loads when browser is idle

Reference: technical-blueprint.md §8.4

**Files likely affected:**
- All Svelte components (update Chart.js imports)
- All Astro pages (update client directives)

---

## [ ] T-020 — Visual polish, animations, and empty states

**Phase:** 3 (Polish)
**Component:** Frontend
**Depends on:** All Phase 2 tickets
**Estimated effort:** M
**Priority:** P2 (nice to have)

**Description:**
Polish the visual design: consistent color palette, smooth transitions, loading states for charts, empty state designs, and a custom 404 page.

**Acceptance criteria:**
- [ ] Consistent color scheme across all pages (league-themed or PL-branded)
- [ ] Each manager has a consistent color used across all charts
- [ ] Chart loading states (skeleton/spinner while Svelte islands hydrate)
- [ ] Empty state designs for pages when data is missing
- [ ] Custom 404 page (`src/pages/404.astro`) with navigation back to dashboard
- [ ] Smooth page transitions (view transitions API or CSS animations)
- [ ] Footer with: last updated time, link to GitHub repo, link to status page

**Technical notes:**
Astro supports View Transitions via `<ViewTransitions />` in the layout. CSS transitions for chart reveals.

**Files likely affected:**
- `src/layouts/Layout.astro`
- `src/pages/404.astro`
- All page and component files (styling)
- `src/styles/global.css` (or Tailwind theme config)

---

## [ ] T-021 — Gameweek navigation slider component

**Phase:** 2 (Full Feature Set)
**Component:** Frontend
**Depends on:** T-010
**Estimated effort:** S
**Priority:** P2 (nice to have)

**Description:**
Build a reusable gameweek slider component that lets users navigate between gameweeks on relevant pages (H2H, trends, team detail).

**Acceptance criteria:**
- [ ] `GameweekSlider.svelte` component with prev/next buttons and GW number display
- [ ] Slider range: GW1 to current completed GW
- [ ] Used on H2H page (show specific GW matchup) and team detail page (show GW roster)
- [ ] Emits selected GW as event for parent components to react

**Technical notes:**
Simple Svelte component with reactive state. Can be a range input or custom button-based slider.

**Files likely affected:**
- `src/components/GameweekSlider.svelte`
- Pages that integrate it

---

## [x] T-022 — Initial data seed run
> DONE 2026-03-12 — Full seed of GW1-29 data committed. All JSON files valid, dashboard.json generated.

**Phase:** 1 (MVP)
**Component:** Data Pipeline
**Depends on:** T-004, T-005, T-002
**Estimated effort:** S
**Priority:** P0 (critical path)

**Description:**
Run the data pipeline for the first time to seed all historical data (GW1-29). Verify all JSON files are produced correctly. Commit the data to the repo.

**Acceptance criteria:**
- [ ] `scripts/fetch-all.ts` executed successfully, fetching all 29 completed GWs
- [ ] All JSON files present in `data/` with valid content
- [ ] `data/meta.json` shows lastFetchedGameweek: 29 (or current)
- [ ] `data/dashboard.json` generated with all sections populated
- [ ] Data committed and pushed to main branch
- [ ] GitHub Actions fetch-and-deploy workflow runs successfully end-to-end
- [ ] Dashboard is accessible on GitHub Pages with real data

**Technical notes:**
This is a manual execution step to seed historical data. After this, the cron takes over.
Run locally or trigger via `workflow_dispatch`.

**Files likely affected:**
- All `data/*.json` files
- `data/picks/*.json`
- `data/live/*.json`

---

## [x] T-023 — Responsive navigation and mobile layout
> DONE 2026-03-12 — Hamburger menu, active page, league name header, live last-updated footer.

**Phase:** 1 (MVP)
**Component:** Frontend
**Depends on:** T-001
**Estimated effort:** M
**Priority:** P0 (critical path)

**Description:**
Implement the responsive navigation menu and ensure the base layout works well on mobile devices.

**Acceptance criteria:**
- [ ] Desktop: horizontal navigation bar with links to all pages
- [ ] Mobile: hamburger menu that opens a slide-out or dropdown nav
- [ ] Active page highlighted in navigation
- [ ] League name "FC Driegangendiner" in the header
- [ ] Footer with last-updated timestamp and GitHub link
- [ ] All page content respects container max-width on desktop, full-width on mobile
- [ ] Tested on Chrome and Safari mobile viewports

**Technical notes:**
Use Tailwind responsive classes (`md:`, `lg:`). The hamburger menu can be a simple Svelte island with toggle state, or pure CSS with a checkbox hack.

**Files likely affected:**
- `src/layouts/Layout.astro`
- `src/components/Nav.svelte` (or Nav.astro with client script)
- `src/components/Footer.astro`

---

## [ ] T-024 — Documentation and CLAUDE.md setup

**Phase:** 1 (MVP)
**Component:** Documentation
**Depends on:** T-001
**Estimated effort:** S
**Priority:** P1 (important)

**Description:**
Create a CLAUDE.md file at the repo root with project context, conventions, and instructions for the engineer agent. Update README with quick start instructions.

**Acceptance criteria:**
- [ ] `CLAUDE.md` created with: project overview, tech stack, folder structure, how to run locally, how to run data pipeline, conventions (TypeScript strict, Tailwind, Astro components vs Svelte islands)
- [ ] README.md updated with quick start instructions
- [ ] Scripts section in package.json documented: `dev`, `build`, `preview`, `test`, `fetch`, `transform`

**Technical notes:**
CLAUDE.md should help an autonomous engineer agent understand the project without reading all documentation files. Key info: league ID 820, 8 managers, the combined workflow pattern, data flow from API to JSON to Astro.

**Files likely affected:**
- `CLAUDE.md`
- `README.md`
- `package.json` (scripts section)

---

## Summary

| Phase | Tickets | P0 | P1 | P2 | Effort Breakdown |
|-------|---------|----|----|----|--------------------|
| Phase 1 (MVP) | T-001 to T-008, T-022, T-023, T-024 | 8 | 2 | 1 | 2S + 5M + 2L + 1XL |
| Phase 2 (Full) | T-009 to T-016, T-021 | 0 | 7 | 2 | 3S + 5M + 1L |
| Phase 3 (Polish) | T-017 to T-020 | 0 | 0 | 4 | 0S + 3M + 1L |
| **Total** | **24** | **8** | **9** | **7** | **5S + 13M + 4L + 1XL** |
